<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unse.gestiondepolideportivo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EnviarDatosReservasActivity"></activity>
        <activity android:name=".ListadoReservaActivity"/>
        <activity android:name=".DialogoNewReservaActivity"/>
        <activity android:name=".GestionReservaActivity"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SplashScreenActivity" />
        <activity android:name=".GestionPiletaActivity" />
        <activity android:name=".ListadoIngresoActivity" />
        <activity android:name=".EnviarDatosPiletaActivity" />
        <activity android:name=".VerificarDatoActivity" />
    </application>

</manifest>